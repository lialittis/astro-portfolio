<!-- Check and apply theme before rendering -->
<script is:inline>
  // Apply stored theme immediately to prevent flash
  (function() {
    const storedStyle = localStorage.getItem('style');
    if (storedStyle && (storedStyle === 'modern' || storedStyle === 'hacker')) {
      document.documentElement.setAttribute('data-style', storedStyle);
      document.body.setAttribute('data-style', storedStyle);
    } else {
      // Default to hacker mode if no preference stored
      document.documentElement.setAttribute('data-style', 'hacker');
      document.body.setAttribute('data-style', 'hacker');
      localStorage.setItem('style', 'hacker');
    }
  })();
</script>

<nav class='navbar-cool'>
  <!-- Brand/Logo -->
  <div class='navbar-brand'>
    <span class='brand-text'>Tianchi YU</span>
  </div>

	<!-- Desktop Navigation -->
	<div class='navbar-desktop'>
		<a class='nav-link' href='/'>Home</a>
		<a class='nav-link' href='/project'>Projects</a>
		<a class='nav-link' href='/me'>About</a>
    <a class='nav-link' href='/blog'>Blogs</a>
		<a class='nav-link' href='/publications'>Publications</a>
		<a class='nav-link' href='/confclock'>Conferences</a>
	</div>

	<!-- Mobile Menu Button -->
	<button id='menu-button' class='mobile-menu-btn'> 
    <span class='hamburger-line'></span>
    <span class='hamburger-line'></span>
    <span class='hamburger-line'></span>
  </button>

	<!-- Mobile Dropdown -->
	<div id='mobile-menu' class='mobile-dropdown hidden'>
		<div class='mobile-nav-content'>
			<a class='mobile-nav-link' href='/'>Home</a>
			<a class='mobile-nav-link' href='/project'>Projects</a>
			<a class='mobile-nav-link' href='/me'>About</a>
			<a class='mobile-nav-link' href='/blog'>Blog</a>
			<a class='mobile-nav-link' href='/publications'>Publications</a>
			<a class='mobile-nav-link' href='/confclock'>Conferences</a>
		</div>
	</div>
</nav>

<style>
  /* Cool Navigation Bar Styling - Default: Hacker Mode */
  .navbar-cool {
    position: fixed;
    top: 50%;
    left: 20px;
    transform: translateY(-50%);
    width: auto;
    max-width: none;
    z-index: 1000;
    
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
    padding: 1.5rem 1rem;
    
    /* Hacker theme as default */
    background: rgba(0, 20, 0, 0.9);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid #00ff41;
    border-radius: 10px;
    box-shadow: 0 0 30px rgba(0, 255, 65, 0.3);
    
    transition: all 0.3s ease;
  }

  .navbar-cool:hover {
    background: rgba(0, 30, 0, 0.95);
    border-color: #00ff41;
    box-shadow: 0 0 40px rgba(0, 255, 65, 0.5);
  }

  /* Brand/Logo Styling - Hacker Default */
  .navbar-brand {
    flex-shrink: 0;
    margin-bottom: 2rem;
  }

  .brand-text {
    font-size: 1.25rem;
    font-weight: 800;
    color: #00ff41;
    background: none;
    -webkit-text-fill-color: #00ff41;
    text-shadow: 0 0 10px rgba(0, 255, 65, 0.5);
    font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
    letter-spacing: -0.02em;
    transition: all 0.3s ease;
  }

  .brand-text:hover {
    color: #00ff41;
    text-shadow: 0 0 15px rgba(0, 255, 65, 0.8);
  }

  /* Desktop Navigation - Hacker Default */
  .navbar-desktop {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    position: static;
    transform: none;
  }

  .nav-link {
    color: #00ff41;
    text-decoration: none;
    font-weight: 500;
    font-size: 0.9rem;
    padding: 0.75rem 1rem;
    border-radius: 5px;
    transition: all 0.3s ease;
    position: relative;
    cursor: pointer;
    font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
    text-align: center;
    min-width: 120px;
    border: 1px solid transparent;
  }

  .nav-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 255, 65, 0.1);
    border-radius: 5px;
    opacity: 0;
    transition: all 0.3s ease;
    z-index: -1;
  }

  .nav-link:hover {
    color: #ffffff;
    background: rgba(0, 255, 65, 0.2);
    border-color: #00ff41;
    text-shadow: 0 0 10px rgba(0, 255, 65, 0.8);
    box-shadow: 0 0 15px rgba(0, 255, 65, 0.3);
  }

  .nav-link:hover::before {
    opacity: 1;
  }

  /* Mobile Menu Button - Hacker Default */
  .mobile-menu-btn {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    width: 2.5rem;
    height: 2.5rem;
    background: rgba(0, 20, 0, 0.9);
    border: 1px solid #00ff41;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.3s ease;
    gap: 0.25rem;
  }

  .mobile-menu-btn:hover {
    background: rgba(0, 30, 0, 0.95);
    transform: translateY(-2px);
    box-shadow: 0 0 20px rgba(0, 255, 65, 0.5);
  }

  .hamburger-line {
    width: 1.2rem;
    height: 2px;
    background: #00ff41;
    border-radius: 1px;
    transition: all 0.3s ease;
    box-shadow: 0 0 5px rgba(0, 255, 65, 0.5);
  }

  /* Mobile Dropdown - Hacker Default */
  .mobile-dropdown {
    position: absolute;
    top: calc(100% + 1rem);
    left: 0;
    right: 0;
    background: rgba(0, 20, 0, 0.95);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid #00ff41;
    border-radius: 10px;
    box-shadow: 0 0 30px rgba(0, 255, 65, 0.3);
    transition: all 0.3s ease;
  }

  .mobile-nav-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1.5rem;
    gap: 1rem;
  }

  .mobile-nav-link {
    color: #00ff41;
    text-decoration: none;
    font-weight: 500;
    font-size: 1rem;
    padding: 0.75rem 1.5rem;
    border-radius: 5px;
    transition: all 0.3s ease;
    width: 100%;
    text-align: center;
    font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
  }

  .mobile-nav-link:hover {
    color: #ffffff;
    background: rgba(0, 255, 65, 0.2);
    text-shadow: 0 0 10px rgba(0, 255, 65, 0.8);
    transform: translateY(-2px);
  }

  /* Responsive Design */
  @media (min-width: 1024px) {
    .navbar-desktop {
      display: flex;
    }
    
    .mobile-menu-btn {
      display: none;
    }
  }

  @media (max-width: 1023px) {
    .navbar-cool {
      width: calc(100% - 1rem);
      padding: 0.75rem 1rem;
    }
    
    .brand-text {
      font-size: 1.1rem;
    }
  }

  @media (max-width: 640px) {
    .navbar-cool {
      top: 0.5rem;
      width: calc(100% - 1rem);
    }
  }

  /* Animation for mobile menu toggle */
  .mobile-dropdown.show {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .mobile-dropdown.hidden {
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
  }

  /* Modern Mode Overrides - Glassmorphism Center-Top */
  :global(body[data-style="modern"]) .navbar-cool {
    position: fixed;
    top: 1.5rem;
    left: 50%;
    transform: translateX(-50%);
    width: auto;
    max-width: 90%;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem 2rem;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.15) 0%, rgba(255, 255, 255, 0.08) 100%);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid rgba(255, 255, 255, 0.25);
    border-radius: 50px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
  }

  :global(body[data-style="modern"]) .navbar-cool:hover {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.2) 0%, rgba(255, 255, 255, 0.1) 100%);
    border-color: rgba(255, 255, 255, 0.3);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
  }

  :global(body[data-style="modern"]) .navbar-brand {
    margin-bottom: 0;
  }

  :global(body[data-style="modern"]) .brand-text {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: none;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }

  :global(body[data-style="modern"]) .brand-text:hover {
    color: inherit;
    text-shadow: none;
  }

  :global(body[data-style="modern"]) .navbar-desktop {
    position: static;
    transform: none;
    flex-direction: row;
    gap: 0.5rem;
    display: flex;
  }

  :global(body[data-style="modern"]) .nav-link {
    color: rgba(255, 255, 255, 0.9);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    font-size: 0.95rem;
    padding: 0.5rem 1.25rem;
    border-radius: 25px;
    text-align: center;
    min-width: auto;
    border: 1px solid transparent;
  }

  :global(body[data-style="modern"]) .nav-link::before {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.3) 0%, rgba(118, 75, 162, 0.3) 100%);
    border-radius: 25px;
  }

  :global(body[data-style="modern"]) .nav-link:hover {
    color: white;
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.3) 0%, rgba(118, 75, 162, 0.3) 100%);
    border-color: transparent;
    text-shadow: none;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
    transform: translateY(-2px);
  }

  :global(body[data-style="modern"]) .mobile-menu-btn {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.15) 0%, rgba(255, 255, 255, 0.08) 100%);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 12px;
  }

  :global(body[data-style="modern"]) .mobile-menu-btn:hover {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.3) 0%, rgba(118, 75, 162, 0.3) 100%);
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
  }

  :global(body[data-style="modern"]) .hamburger-line {
    background: white;
    box-shadow: none;
  }

  :global(body[data-style="modern"]) .mobile-dropdown {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.15) 0%, rgba(255, 255, 255, 0.08) 100%);
    border: 1px solid rgba(255, 255, 255, 0.25);
    border-radius: 20px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }

  :global(body[data-style="modern"]) .mobile-nav-link {
    color: rgba(255, 255, 255, 0.9);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }

  :global(body[data-style="modern"]) .mobile-nav-link:hover {
    color: white;
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.3) 0%, rgba(118, 75, 162, 0.3) 100%);
    text-shadow: none;
    transform: translateY(-2px);
  }

  @media (max-width: 1023px) {
    :global(body[data-style="modern"]) .navbar-cool {
      top: 1rem;
      left: 50%;
      transform: translateX(-50%);
      flex-direction: row;
      padding: 0.75rem 1rem;
      width: calc(100% - 1rem);
    }

    :global(body[data-style="modern"]) .navbar-desktop {
      display: none;
    }
  }

  /* Hacker Mode Styles */
  :global(body[data-style="hacker"]) .navbar-cool {
    position: fixed;
    top: 50%;
    left: 20px;
    transform: translateY(-50%);
    width: auto;
    max-width: none;
    flex-direction: column;
    padding: 1.5rem 1rem;
    background: rgba(0, 20, 0, 0.9);
    border: 1px solid #00ff41;
    border-radius: 10px;
    box-shadow: 0 0 30px rgba(0, 255, 65, 0.3);
  }

  :global(body[data-style="hacker"]) .navbar-cool:hover {
    background: rgba(0, 30, 0, 0.95);
    border-color: #00ff41;
    box-shadow: 0 0 40px rgba(0, 255, 65, 0.5);
  }

  :global(body[data-style="hacker"]) .navbar-brand {
    margin-bottom: 2rem;
  }

  :global(body[data-style="hacker"]) .brand-text {
    color: #00ff41;
    background: none;
    -webkit-text-fill-color: #00ff41;
    text-shadow: 0 0 10px rgba(0, 255, 65, 0.5);
    font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
  }

  :global(body[data-style="hacker"]) .navbar-desktop {
    position: static;
    transform: none;
    flex-direction: column;
    gap: 0.5rem;
    display: flex;
  }

  :global(body[data-style="hacker"]) .nav-link {
    color: #00ff41;
    font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
    font-size: 0.9rem;
    padding: 0.75rem 1rem;
    border-radius: 5px;
    text-align: center;
    min-width: 120px;
    border: 1px solid transparent;
    transition: all 0.3s ease;
  }

  :global(body[data-style="hacker"]) .nav-link::before {
    background: rgba(0, 255, 65, 0.1);
    border-radius: 5px;
  }

  :global(body[data-style="hacker"]) .nav-link:hover {
    color: #ffffff;
    background: rgba(0, 255, 65, 0.2);
    border-color: #00ff41;
    text-shadow: 0 0 10px rgba(0, 255, 65, 0.8);
    box-shadow: 0 0 15px rgba(0, 255, 65, 0.3);
  }

  :global(body[data-style="hacker"]) .mobile-menu-btn {
    background: rgba(0, 20, 0, 0.9);
    border: 1px solid #00ff41;
    border-radius: 5px;
  }

  :global(body[data-style="hacker"]) .hamburger-line {
    background: #00ff41;
    box-shadow: 0 0 5px rgba(0, 255, 65, 0.5);
  }

  :global(body[data-style="hacker"]) .mobile-dropdown {
    background: rgba(0, 20, 0, 0.95);
    border: 1px solid #00ff41;
    border-radius: 10px;
    box-shadow: 0 0 30px rgba(0, 255, 65, 0.3);
  }

  :global(body[data-style="hacker"]) .mobile-nav-link {
    color: #00ff41;
    font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
  }

  :global(body[data-style="hacker"]) .mobile-nav-link:hover {
    color: #ffffff;
    background: rgba(0, 255, 65, 0.2);
    text-shadow: 0 0 10px rgba(0, 255, 65, 0.8);
  }

  @media (max-width: 1023px) {
    :global(body[data-style="hacker"]) .navbar-cool {
      top: 20px;
      left: 10px;
      transform: none;
      flex-direction: row;
      padding: 0.75rem 1rem;
    }

    :global(body[data-style="hacker"]) .navbar-brand {
      margin-bottom: 0;
    }

    :global(body[data-style="hacker"]) .navbar-desktop {
      display: none;
    }
  }
</style>

<script>
	const menuButton = document.getElementById('menu-button')
	const mobileMenu = document.getElementById('mobile-menu')

	if (menuButton != null && mobileMenu != null) {
		menuButton.addEventListener('click', () => {
			if (mobileMenu.classList.contains('hidden')) {
				mobileMenu.classList.remove('hidden')
				mobileMenu.classList.add('show')
			} else {
				mobileMenu.classList.remove('show')
				mobileMenu.classList.add('hidden')
			}
		})

		// Close mobile menu when clicking outside
		document.addEventListener('click', (event) => {
			const target = event.target as Element
			if (target && !menuButton.contains(target) && !mobileMenu.contains(target)) {
				mobileMenu.classList.remove('show')
				mobileMenu.classList.add('hidden')
			}
		})

		// Close mobile menu on window resize to desktop size
		window.addEventListener('resize', () => {
			if (window.innerWidth >= 1024) {
				mobileMenu.classList.remove('show')
				mobileMenu.classList.add('hidden')
			}
		})
	}
</script>
